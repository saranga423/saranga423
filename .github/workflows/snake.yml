name: snake

on:
  schedule:
    - cron: "0 0 * * *"

jobs:
  snake:
    runs-on: ubuntu-latest
    steps:
      - uses: Platane/snk@v3
        with:
          github_user_name: saranga423
          outputs: dist/github-contribution-grid-snake.svg
